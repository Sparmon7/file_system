Studio 21 ReadMe.txt 

1. Ethan Weilheimer, Garrett Ramsey, Spencer Armon

2.So it can properly destroy any derived classes and prevent memory leaks instead of deleting only the base class.

3. This makes it so command prompt works with any factory, system, or object, making it very flexible since it won't have to change for new types.
This is very easy to use.

4. We created a command prompt and tested the following things in the command prompt inside the command prompt: gibberish, help, help gibber, help touch, touch, touch image.img, touch image.txt, touch image.g, touch fakefile
They all worked as expected :)